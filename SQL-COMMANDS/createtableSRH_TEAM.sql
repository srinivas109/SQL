 CREATE TABLE SRH_TEAM(
   ID   INT              NOT NULL,
   NAME VARCHAR (20)     NOT NULL,
   DOB  INT              NOT NULL,
   POB  CHAR (25) ,
NOMATCHES INT,
   SALARY   DECIMAL (18, 2),       
   PRIMARY KEY (ID)
);